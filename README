# wasm_el_seba_automation

UI test automation project for Wasm El Seba website using Java + Selenium + TestNG + Maven.

## What is inside
- Common Pages tests
- Mushark Login tests
- Mushark SignUp tests
- Voter Login tests
- Voter Register tests

## CI (GitHub Actions)
Tests run automatically on every push to the repository (GitHub Actions workflow is in `.github/workflows/`).

## Reports
After running tests:
- `target/surefire-reports/`
